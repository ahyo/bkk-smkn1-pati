name: Deploy demo ke GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - "docs/**"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Periksa sintaks JavaScript demo
        run: |
          node --check docs/assets/demo-app.js
          node --check docs/assets/demo-data.js
          node --check docs/assets/icons.js

      - name: Pastikan berkas wajib ada
        run: |
          test -f docs/index.html
          test -f docs/assets/style.css
          test -f docs/assets/demo.css
          test -f docs/assets/icons.js
          test -f docs/.nojekyll

      - uses: actions/configure-pages@v6
      - uses: actions/upload-pages-artifact@v5
        with:
          path: docs

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
